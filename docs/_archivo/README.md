# Archivo

Specs que ya no están en curso. No son la feature activa y no hay que implementarlos.

- `2026-09-04-app-finanzas-v1/` — primer intento de la app de finanzas. Nunca se implementó
  (las 29 secciones de bitácora están todas en placeholder). Se conserva porque es el único
  spec grande que hay: 6 requisitos y 29 tareas, que es lo que hace falta para ejercitar dos
  cosas que un spec chico no puede probar — la derivación de ids `R<n>.<m>` a través de varios
  requisitos, y un fan-out con margen para que un revisor disienta.
